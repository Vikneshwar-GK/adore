name: adore
version: "1.0.0"
profile: adore

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  adore:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: warehouse
    warehouse:
      +materialized: table
      +schema: warehouse
